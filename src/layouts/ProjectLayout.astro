---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  tagline?: string;
  image?: string;
}

const { title, tagline, image } = Astro.props;
---

<BaseLayout title={title} description={tagline} image={image}>
  <slot />
</BaseLayout>
